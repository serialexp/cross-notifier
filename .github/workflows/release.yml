name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: false
        default: 'v0.0.0-dev'

env:
  GO_VERSION: '1.25.4'

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libx11-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev \
            libasound2-dev \
            libgtk-3-dev

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Build
        run: |
          VERSION=${{ steps.version.outputs.version }}
          go build -ldflags "-X main.Version=${VERSION}" -o cross-notifier .

      - name: Package
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p dist
          tar -czvf dist/cross-notifier-${VERSION}-linux-amd64.tar.gz \
            cross-notifier \
            README.md

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-amd64
          path: dist/*.tar.gz

  build-linux-compat:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libx11-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxi-dev \
            libxxf86vm-dev \
            libasound2-dev \
            libgtk-3-dev

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Build
        run: |
          VERSION=${{ steps.version.outputs.version }}
          go build -ldflags "-X main.Version=${VERSION}" -o cross-notifier .

      - name: Package
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p dist
          tar -czvf dist/cross-notifier-${VERSION}-linux-amd64-compat.tar.gz \
            cross-notifier \
            README.md

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-amd64-compat
          path: dist/*.tar.gz

  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Generate app icon
        run: |
          mkdir -p Icon.iconset
          sips -z 16 16 logo.png --out Icon.iconset/icon_16x16.png
          sips -z 32 32 logo.png --out Icon.iconset/icon_16x16@2x.png
          sips -z 32 32 logo.png --out Icon.iconset/icon_32x32.png
          sips -z 64 64 logo.png --out Icon.iconset/icon_32x32@2x.png
          sips -z 128 128 logo.png --out Icon.iconset/icon_128x128.png
          sips -z 256 256 logo.png --out Icon.iconset/icon_128x128@2x.png
          sips -z 256 256 logo.png --out Icon.iconset/icon_256x256.png
          sips -z 512 512 logo.png --out Icon.iconset/icon_256x256@2x.png
          sips -z 512 512 logo.png --out Icon.iconset/icon_512x512.png
          sips -z 1024 1024 logo.png --out Icon.iconset/icon_512x512@2x.png
          iconutil -c icns Icon.iconset -o Icon.icns
          rm -rf Icon.iconset

      - name: Build
        env:
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 1
          MACOSX_DEPLOYMENT_TARGET: '11.0'
        run: |
          VERSION=${{ steps.version.outputs.version }}
          go build -ldflags "-X main.Version=${VERSION}" -o cross-notifier .

      - name: Create app bundle
        run: |
          VERSION=${{ steps.version.outputs.version }}
          APP_NAME="CrossNotifier"
          APP_DIR="${APP_NAME}.app"

          mkdir -p "${APP_DIR}/Contents/MacOS"
          mkdir -p "${APP_DIR}/Contents/Resources"

          cp cross-notifier "${APP_DIR}/Contents/MacOS/"

          if [ -f "Icon.icns" ]; then
            cp Icon.icns "${APP_DIR}/Contents/Resources/"
          fi

          cat > "${APP_DIR}/Contents/Info.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>cross-notifier</string>
              <key>CFBundleIconFile</key>
              <string>Icon</string>
              <key>CFBundleIdentifier</key>
              <string>com.crossnotifier.app</string>
              <key>CFBundleName</key>
              <string>${APP_NAME}</string>
              <key>CFBundleDisplayName</key>
              <string>${APP_NAME}</string>
              <key>CFBundleVersion</key>
              <string>${VERSION#v}</string>
              <key>CFBundleShortVersionString</key>
              <string>${VERSION#v}</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleSignature</key>
              <string>????</string>
              <key>LSMinimumSystemVersion</key>
              <string>11.0</string>
              <key>NSHighResolutionCapable</key>
              <true/>
              <key>LSUIElement</key>
              <false/>
          </dict>
          </plist>
          EOF

      - name: Create DMG
        run: |
          VERSION=${{ steps.version.outputs.version }}
          ARCH=${{ matrix.arch }}
          APP_NAME="CrossNotifier"
          DMG_NAME="CrossNotifier-${VERSION}-macos-${ARCH}.dmg"

          mkdir -p dmg-temp
          cp -R "${APP_NAME}.app" dmg-temp/
          ln -s /Applications dmg-temp/Applications

          mkdir -p dist
          hdiutil create -volname "${APP_NAME}" -srcfolder dmg-temp -ov -format UDZO "dist/${DMG_NAME}"
          rm -rf dmg-temp

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.arch }}
          path: dist/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Get version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Build
        shell: bash
        run: |
          VERSION=${{ steps.version.outputs.version }}
          go build -ldflags="-H windowsgui -X main.Version=${VERSION}" -o cross-notifier.exe .

      - name: Package
        shell: bash
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p dist
          7z a -tzip "dist/cross-notifier-${VERSION}-windows-amd64.zip" \
            cross-notifier.exe \
            README.md

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-amd64
          path: dist/*.zip

  build-server:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - arch: amd64
            goarch: amd64
          - arch: arm64
            goarch: arm64
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Build server
        env:
          CGO_ENABLED: 0
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -o server ./cmd/server

      - name: Package
        run: |
          VERSION=${{ steps.version.outputs.version }}
          mkdir -p dist
          tar -czvf dist/cross-notifier-server-${VERSION}-linux-${{ matrix.arch }}.tar.gz \
            server \
            README.md

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-linux-${{ matrix.arch }}
          path: dist/*.tar.gz

  build-docker:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          echo "version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Set up Depot CLI
        uses: depot/setup-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: depot/build-push-action@v1
        with:
          project: ${{ secrets.DEPOT_PROJECT_ID }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            aeolun/cross-notifier-server:latest
            aeolun/cross-notifier-server:${{ steps.version.outputs.version }}
          provenance: true
          sbom: true

  release:
    needs: [build-linux, build-linux-compat, build-macos, build-windows, build-server, build-docker]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/linux-amd64/*
            artifacts/linux-amd64-compat/*
            artifacts/macos-amd64/*
            artifacts/macos-arm64/*
            artifacts/windows-amd64/*
            artifacts/server-linux-amd64/*
            artifacts/server-linux-arm64/*
          generate_release_notes: true
