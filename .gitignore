# Binary
cross-notifier

# Rust build artifacts
target/

# macOS build artifacts
*.app
*.dmg
dmg-temp/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store

# iOS — xcodegen regenerates the project on demand, so committing it
# adds noise and merge conflicts without benefit. Same goes for build
# output and per-user Xcode state.
ios/*.xcodeproj
ios/*.xcworkspace
ios/build/
ios/DerivedData/
ios/**/xcuserdata/
ios/**/*.xcuserstate

# Claude Code local settings
.claude/settings.local.json
.env
node_modules/
